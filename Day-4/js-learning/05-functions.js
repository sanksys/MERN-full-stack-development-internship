/*
FUNCTIONS
*/

function add(x, y) {
  return x + y;
}

const multiply1 = (a, b) => a * b;

/*
FUNCTIONS
---------
Normal vs Arrow functions
*/

// Normal
function add(a, b) {
  return a + b;
}

// Arrow (React default)
const multiply = (a, b) => a * b;

/*
Arrow functions:
- Short syntax
- Lexical this
- Used everywhere in React
*/
